# Lab 05 (M55) - console output, hwinfo for the boot-time reset-cause
# dispatch (Lab 02/04 pattern), and the mbox link to M4 (Lab 03 pattern,
# reused unchanged).

CONFIG_PRINTK=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_SERIAL=y

CONFIG_HWINFO=y
CONFIG_MBOX=y

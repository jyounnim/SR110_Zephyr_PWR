# Lab 03 (M55) - console output plus the mbox link to M4.
#
# M55 only listens for M4's motion-state notifications in this lab -- it
# never sends anything back, so only the rx side of mbox is used.

CONFIG_PRINTK=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_SERIAL=y

CONFIG_MBOX=y
